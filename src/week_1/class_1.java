package week_1;

public class class_1 {

    int a;  // 멤버변수 : 클래스 내부에 선언된 변수

    //인스턴스 메서드 -> static이 사용되지 않은 메서드
    //반드시 객체를 생성한 후 호출 가능
    //인스턴스 메서드는 같은 클래스내의 인스턴스 메서드를 직접 호출 가능
    void isPrime(){
        int b ; // 지역변수 : 메서드 내부에 선언된 변수
    }

    //정적(static) 메서드 : static 예약어가 붙어진 메서드
    //객체 생성 없이 클래스의 이름만으로 호출 가능
    //정적 메서드는 같은 클래스내에서도 인스턴스 메서들르 직접 호출할 수 없음
    public static void main(String[] args) {
        //객체를 생성하기 위해서는
        // 1) 객체 참조 변수를 선언
        // 2) new 연산자를 이용하여 객체를 생성
        // 3) ' = ' 를 이용하여 참조 변수에 객체를 대입

        // 객체 참조 변수의 타입은 객체로 생성할 클래스의 이름
        class_1  obj = new class_1();
        obj.isPrime();

    }
}
